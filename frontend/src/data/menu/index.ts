import type { Product, Category, Ingredient } from '../../types/pos';

export const initialIngredients: Ingredient[] = [];
export const initialCategories: Category[] = [];
export const initialProducts: Product[] = [];
